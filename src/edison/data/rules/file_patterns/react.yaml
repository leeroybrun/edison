id: FILE_PATTERN.REACT_COMPONENT
title: Component TSX files trigger react validator
patterns:
  - "**/components/**/*.tsx"
validators:
  - react
rationale: >-
  React components require validation for hooks usage, accessibility, and render patterns, so component TSX files trigger the react validator.

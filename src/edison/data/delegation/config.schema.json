{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Delegation Config Schema (Alias)",
  "description": "Alias schema that references the core validators schema until a dedicated delegation schema is published.",
  "$ref": "../validators/config.schema.json"
}

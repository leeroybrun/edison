context7:
  version: "1.0.0"
  trainingCutoff: "2025-01"

  # Core is technology-agnostic. Packs/projects provide triggers, aliases,
  # content detection, and package definitions via config overlays.
  triggers: {}
  aliases: {}
  contentDetection: {}
  packages: {}

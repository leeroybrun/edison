{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.edison/core/claude-agent.schema.json",
  "title": "Claude Code Agent Document",
  "description": "Structured representation of a Claude Code agent generated by Edison.",
  "type": "object",
  "required": ["name", "description", "model", "sections"],
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Exported agent name used by Claude Code."
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "description": "One-line summary of the agent role."
    },
    "model": {
      "type": "string",
      "minLength": 1,
      "description": "Claude model identifier (e.g. sonnet, haiku)."
    },
    "role": {
      "type": "string",
      "description": "Optional role metadata surfaced in frontmatter."
    },
    "tags": {
      "type": "array",
      "description": "Optional tags applied to the agent.",
      "items": { "type": "string", "minLength": 1 }
    },
    "sections": {
      "type": "object",
      "description": "Logical sections extracted from the Edison-composed agent body.",
      "required": ["role", "tools", "guidelines", "workflows"],
      "additionalProperties": false,
      "properties": {
        "role": {
          "type": "string",
          "minLength": 1,
          "description": "Full text of the Role section."
        },
        "tools": {
          "type": "string",
          "description": "Tools section content (may be empty when not applicable)."
        },
        "guidelines": {
          "type": "string",
          "description": "Guidelines section content (may be empty when not applicable)."
        },
        "workflows": {
          "type": "string",
          "description": "Workflows section content (may be empty when not applicable)."
        }
      }
    }
  }
}


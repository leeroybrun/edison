# Core Example References (moved from Edison core)

- File path patterns typical for Next.js app routes: `apps/<app>/src/app/api/auth/route.ts`

Note: These examples are stack-specific and therefore live in the Next.js pack, not in Edison core schemas.


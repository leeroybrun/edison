# Core Example References (moved from Edison core)

- Validator trigger glob examples: `**/*.tsx`
- Package/topic examples: `react`

Note: These examples are stack-specific and therefore live in the React pack, not in Edison core schemas.


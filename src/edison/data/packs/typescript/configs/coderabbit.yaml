# TypeScript pack overlay for CodeRabbit

path_instructions:
  - path: "**/*.ts"
    instructions: |
      Review TypeScript code for:
      - Type safety and proper type annotations
      - Avoid 'any' types where possible
      - Use of strict mode features
      - Proper interface/type definitions
      - Generic usage and constraints

  - path: "**/*.tsx"
    instructions: |
      Review TypeScript React components for:
      - Proper component typing (FC, props interfaces)
      - Type-safe event handlers
      - Correct hook typing (useState, useEffect, etc.)
      - Props interface documentation

  - path: "**/tsconfig*.json"
    instructions: |
      Review TypeScript configuration for:
      - Strict mode settings
      - Module resolution strategy
      - Target and lib settings
      - Path aliases configuration

# Type Safety Patterns

- Prefer discriminated unions over optional fields.
- Use `satisfies` for config objects to prevent excess properties.
- Constrain generics (e.g., `<T extends Record<string, unknown>>`).


# Advanced Types

- Use `satisfies` to verify object shape without widening.
- Constrain generics with `extends` and default type args.
- Prefer `as const` for literal inference; avoid overuse of `as` assertions.
- Use template literal types for expressive keys.


# Core Example References (moved from Edison core)

- Validator trigger glob examples: `schema.prisma`

Note: These examples are stack-specific and therefore live in the Prisma pack, not in Edison core schemas.


# Schema Design Patterns

- Use UUID primary keys; prefer explicit relations with `@relation`.
- Normalize appropriately; avoid excessive denormalization.
- Use enums for constrained value sets.
- Add unique constraints and indexes as needed.


name: Motion
version: 1.0.0
description: Motion (framer-motion/motion) animations and gesture handling for React components

triggers:
  filePatterns:
    - "**/*.tsx"
    - "**/*.jsx"
    - "**/*.ts"
    - "**/motion/**/*"
    - "**/*animation*"

provides:
  agents:
    - agents/component-builder.md
  validators:
    - validators/motion.md

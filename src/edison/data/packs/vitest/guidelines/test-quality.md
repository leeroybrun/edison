# Test Quality

- Use descriptive `describe`/`it` names; assert behavior, not implementation.
- Prefer user-facing assertions for components; avoid brittle snapshots.
- Ensure proper setup/teardown; avoid shared mutable state.


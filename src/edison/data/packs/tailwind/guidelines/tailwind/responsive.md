# Responsive Patterns

- Use mobile-first styles; apply breakpoints progressively.
- Prefer container queries where available; avoid fragile CSS overrides.
- Test across common device sizes and high-contrast modes.


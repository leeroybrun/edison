---
description: List Edison tasks ready to be claimed
agent: build
---
List tasks that are unblocked and ready to be claimed.

```bash
edison task ready
```

Optional: filter to a session:

```bash
edison task ready --session <session-id>
```

---
description: "{{ short_desc | replace('\"', '\\\"') }}"
edison-generated: true
edison-id: "{{ command.id }}"
edison-platform: "{{ platform }}"
{% set platform_config = platform_config | default({'allow_bash': true}) %}
{% if args %}
argument-hint: "{% for arg in args %}{{ arg.name }}{% if not loop.last %} {% endif %}{% endfor %}"
{% endif %}
{% if platform_config.allow_bash %}
allowed-tools: [bash]
{% endif %}
---

# {{ name }}

{{ full_desc }}

```bash
{% if platform_config.allow_bash %}!{% endif %}{{ cli }}
```

{% if args %}
## Arguments
{% for arg in args %}
- **{{ arg.name }}**{% if arg.required %} (required){% endif %}: {{ arg.description }}
{% endfor %}
{% endif %}

## When to use

{{ when_to_use }}

{% if related_commands %}
## Related Commands
{% for cmd in related_commands %}
- /{{ cmd }}
{% endfor %}
{% endif %}

#!/usr/bin/env bash
# Edison Hook: {{ id }}
# Type: {{ type }}
# Description: {{ description }}

set -e

echo "🚀 Edison Session Starting..."

# Initialize session context
SESSION_ID=$(edison session status --json 2>/dev/null | jq -r '.id' || echo "unknown")

echo "   Session: $SESSION_ID"
echo ""

exit 0

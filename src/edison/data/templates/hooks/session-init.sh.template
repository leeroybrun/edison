#!/usr/bin/env bash
# Edison Hook: {{ id }}
# Type: {{ type }}
# Description: {{ description }}

# Fast check: Skip if no Edison session file exists
SESSION_FILE=".project/.session-id"
if [[ ! -f "$SESSION_FILE" ]]; then
  exit 0  # No Edison session, skip hook
fi

echo "🚀 Edison Session Starting..."

# Initialize session context
SESSION_ID=$(edison session status --json 2>/dev/null | jq -r '.id' 2>/dev/null || echo "unknown")

echo "   Session: $SESSION_ID"
echo ""

exit 0

"""Session continuation controls.

Manage per-session continuation overrides stored in `session.meta.continuation`.
This enables clients (OpenCode plugin, hooks) to enforce FC/RL consistently from
`edison session next` output.
"""


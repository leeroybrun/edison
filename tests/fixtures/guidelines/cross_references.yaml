entries:
  - source: orchestrators/SESSION_WORKFLOW.md
    expected:
      - "{{fn:project_config_dir}}/_generated/guidelines/orchestrators/SESSION_WORKFLOW.md"
    forbidden:
      - ".edison/core/guidelines/"
  - source: agents/DELEGATION_AWARENESS.md
    expected:
      - "edison read AVAILABLE_AGENTS"
      - "edison read DELEGATION --type guidelines/shared"
    forbidden:
      - ".edison/core/guidelines/"
  - source: agents/MANDATORY_WORKFLOW.md
    expected:
      - "{{fn:evidence_root}}/<task-id>/round-<N>/{{config.validation.artifactPaths.implementationReportFile}}"
      - "edison read implementation-report.schema.yaml --type schemas/reports"
    forbidden:
      - ".edison/core/guidelines/"
